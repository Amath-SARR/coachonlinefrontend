// import { selectSubscriptionDomain } from '../selectors';

describe('selectSubscriptionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
